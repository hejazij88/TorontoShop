@model List<TorontoShop.Domain.ViewModel.Site.Products.ProductItemViewModel>

@if (Model != null && Model.Any())
{
    @foreach (var product in Model)
    {
        <partial name="_ProductBox" model="product" />
    }

}